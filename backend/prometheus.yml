global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'smart-restaurant-backend'
    scrape_interval: 5s
    static_configs:
      - targets: ['host.docker.internal:5000'] # Connects to backend running on host machine via Docker bridge
    metrics_path: '/metrics'
